import { LoanCalculator } from '../../application/ports/services/LoanCalculator';

/**
 * Implémentation standard du calculateur de prêt
 * Utilise les formules classiques de calcul de mensualités avec amortissement constant
 */
export class StandardLoanCalculator extends LoanCalculator {
  // Hérite des méthodes de la classe LoanCalculator
  // qui contient déjà toute la logique de calcul
}
